class User {
  constructor(id, fullName, email, role, avatarId) {
    this.id = id;
    this.fullName = fullName;
    this.email = email;
    this.role = role;
    this.avatarId = avatarId;
  }
}

export default User;
